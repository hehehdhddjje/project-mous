{
  "name": "project-mous",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "check": "node --check src/main.js && node --check src/coach.js"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@babylonjs/core": "^9.23.0"
  }
}
